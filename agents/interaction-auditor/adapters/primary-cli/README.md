# Primary CLI Adapter

Status: skeleton.

This adapter package is reserved for the primary command-line host integration. Native install and execution instructions will live in this adapter package when the host adapter is implemented.
